#
# This file contains gdb commands for the master(kernel) gdb session
# the trace.sh script replaces the _SYSCALL_ occurrences with the 
# name of the syscall for which we are creating an execution trace.
#
file vmlinux
set pagination off
target remote u:8864

python
import breakpoint
breakpoint.load('_SYSCALL_')
breakpoint.STBreakpoint('_SYSCALL_')
end

continue
